# Defaults OAR pour les smoke tests (file jour, walltime court dans config_smoke.yaml).

chicoree:
  max_jobs: 4
  oar_types: [day]
  oar_resources: host=1/gpu=1

chuc:
  max_jobs: 4
  oar_types: [day]
  oar_resources: host=1/gpu=1

sirius:
  max_jobs: 1
  oar_types: [day]
  oar_resources: host=1/gpu=1
