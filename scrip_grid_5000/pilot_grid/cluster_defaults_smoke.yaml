# Smoke test — file jour + exotic pour chicoree/sirius (clusters non-default).

chicoree:
  max_jobs: 4
  oar_types: [exotic, day]
  oar_resources: host=1/gpu=1

chuc:
  max_jobs: 4
  oar_types: [day]
  oar_resources: host=1/gpu=1

sirius:
  max_jobs: 1
  oar_types: [exotic, day]
  oar_resources: host=1/gpu=1
