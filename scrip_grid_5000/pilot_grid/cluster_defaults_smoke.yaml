# Defaults OAR smoke AUTO — parallélisme réel (file + GPU complets).

chicoree:
  max_jobs: 4
  oar_types: [exotic, day]
  oar_resources: host=1/gpu=4

chuc:
  max_jobs: 4
  oar_types: [day]
  oar_resources: host=1/gpu=4

sirius:
  max_jobs: 8
  oar_types: [exotic, day]
  oar_resources: host=1/gpu=8
